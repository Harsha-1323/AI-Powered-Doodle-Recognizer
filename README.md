# Build a web app where users draw doodles on a canvas, and the app predicts what the doodle represents using a trained ML model

# RUN The App
docker-compose up --build

Visit: http://localhost:5000
